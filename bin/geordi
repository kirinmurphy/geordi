#!/usr/bin/env python3
"""Resolve the canonical checkout even when invoked through installed symlinks."""

from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "cli"))
from geordi_cli.dispatch import main

if __name__ == "__main__":
    sys.exit(main(root=ROOT))
